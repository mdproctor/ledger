<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.casehub</groupId>
    <artifactId>casehub-ledger-signing</artifactId>
    <version>0.2-SNAPSHOT</version>
  </parent>

  <artifactId>casehub-ledger-aws-kms</artifactId>

  <name>CaseHub Ledger - AWS KMS Signing Client</name>
  <description>Pure Java client for AWS KMS signing.
    No CDI, no Quarkus — usable from any framework or plain main().</description>

  <dependencies>

    <!-- Core signing types — AbstractCachingAgentSigner, AgentSignature -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-ledger-core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- AWS KMS SDK -->
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>kms</artifactId>
      <version>${aws-kms-sdk.version}</version>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler-plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire-plugin.version}</version>
      </plugin>
    </plugins>
  </build>

</project>
