# Hibernate ORM is disabled at augmentation (package phase) because quarkus-jdbc-h2
# is test-scoped and unavailable to quarkus:build. The @QuarkusTest execution phase
# reads src/test/resources/application.properties which enables Hibernate ORM with
# a real in-memory H2 datasource.
quarkus.hibernate-orm.enabled=false
